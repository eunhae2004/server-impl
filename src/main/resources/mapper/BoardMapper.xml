<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.koreait.testboard.repository.BoardMapper">

    <insert id="insert">
        INSERT INTO board (title, content, writer_id)
        VALUES (#{title}, #{content}, #{writerId})
    </insert>

    <select id="findById" resultType="Board">
        SELECT * FROM board WHERE id = #{id}
    </select>

    <select id="findAll" resultType="Board">
        SELECT * FROM board ORDER BY created_at DESC
            LIMIT 10 OFFSET #{offset}
    </select>

    <update id="update">
        UPDATE board SET title = #{title}, content = #{content}
        WHERE id = #{id} AND writer_id = #{writerId}
    </update>

    <delete id="delete">
        DELETE FROM board WHERE id = #{id} AND writer_id = #{writerId}
    </delete>

</mapper>
